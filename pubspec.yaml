name: planit
description: "A new Flutter project."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  url_launcher: ^6.1.10
     
  webview_flutter: ^4.2.2  # 최신 버전으로 변경될 수 있음
  webview_flutter_android: ^3.9.0 # Android 플랫폼을 위한 패키지  
  cupertino_icons: ^1.0.8
  http: ^1.4.0
  shared_preferences: ^2.3.2   # ← JWT/간단 설정 저장

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
